interface:
  display_name: "Start"
  short_description: "Handle enriched routing handoffs"
  default_prompt: "Use $start to route this already-enriched request into the right next step."
policy:
  allow_implicit_invocation: false
